name: sisfall

raw_dir: data/raw/${.name}/
processed_dir: data/processed/${.name}/

batch_size: 32
sampling_rate: 200  # Hz
segment_seconds: 3
overlap: 0.5

normalize: zscore

split: 
  train: 0.7
  val: 0.15
  test: 0.15

random_seed: 42